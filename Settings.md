- `settings-%name` (saved)
- dict: id -> value
- types of setting (tag: id)
	- toggle: 1 or 0
- tags to set when creating (on the item)
	- `type`: any of [toggle]
	- `default`: default value
	- `id`: setting id

